import 'package:flutter/material.dart';

class Caratula extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(child: Image.asset('datos/fondo.jpg', fit: BoxFit.contain));
}
